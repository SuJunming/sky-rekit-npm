import React, { useState, useEffect } from 'react'
import { connect } from 'dva'
import { Input } from 'antd'
<% if (style) { %>
import './index.scss'
<% } %>
const <%- name %> = (props: any) => {
    useEffect(() => {}, [])

 
    return <div className='<%- styleName %>_page'><%- name %></div>
}
<% if (model) { %>
export default connect(({ <%- name %> }: any) => ({ <%- name %> }))(<%- name %>)
<% } %>
<% if (!model) { %>
export default <%- name %>
<% } %>
  