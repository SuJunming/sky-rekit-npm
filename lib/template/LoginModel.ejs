import { request, Api } from "@api";
export default {
  namespace: 'Login',
  state: {
  },
  effects: {
    async login({ payload }: any, { put }: any) {
      const res = await request.get(
        '/api/v1/camera/get/11',
        // params,
      )
      put({
        type: 'update',
        payload: res.data,
      })
    },
  },
  reducers: {
    update(state: any, action: any) {
      return {
        ...state,
        ...action.payload,
      }
    },
  },
}