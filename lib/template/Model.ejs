import { request, Api } from "@api";
export default {
  namespace: '<%- name %>',
  state: {
  },
  effects: {},
  reducers: {
   update(state:any, action:any) {
      return {
        ...state,
        ...action.payload,
      }
    },
  },
}
