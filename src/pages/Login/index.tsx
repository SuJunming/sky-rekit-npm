import React, { useState, useEffect } from 'react'
import C from "@C";
import { Input } from 'antd'

import './index.scss'

const Login = (props: any) => {
    useEffect(() => {}, [])

 
    return <div className='login_page'>Login</div>
}

export default C({
    modal: 'Login',
    Component: Login,
    title: 'Login'
})


  